import { sendSuccess } from '../../core/http/responses.js';
export function createPurchaseOrdersController(service){const context=(r)=>({actorUserId:r.auth.userId,requestId:r.id,companyId:r.tenant.companyId,membershipId:r.tenant.membershipId});const mutate=(method)=>async(r,s)=>sendSuccess(s,await service[method](r.tenant.companyId,r.validated.params.id,r.validated.body,context(r)));return{async list(r,s){const value=await service.list(r.tenant.companyId,r.validated.query);return sendSuccess(s,value.purchaseOrders,{meta:{pagination:value.pagination}});},async get(r,s){return sendSuccess(s,await service.get(r.tenant.companyId,r.validated.params.id));},async generate(r,s){return sendSuccess(s,await service.generate(r.tenant.companyId,r.validated.body,context(r)),{statusCode:201});},submit:mutate('submit'),approve:mutate('approve'),send:mutate('send'),cancel:mutate('cancel')};}
